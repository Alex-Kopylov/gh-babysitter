# Доставка событий

## Принцип: не слушаешь — не получаешь

Доставка — это поток в консоль. Сервис пушит события только в **открытые** SSE-соединения; офлайн-клиент события пропускает, и это осознанный принцип дизайна, а не ограничение. Следствия:

- **At-most-once.** Нет очереди, нет replay, нет истории, нет подтверждений доставки.
- События не касаются диска вообще — единственный state сервиса это таблица подписок ([architecture](01-architecture.md#модель-данных)).
- Ретраи доставки не нужны: push в открытое соединение либо прошёл, либо соединение мертво — тогда клиент переподключится и переобъявит подписки.

## Транспорт: SSE

`GET /events/stream` через `sse-starlette`. Почему SSE, а не WebSocket:

- Поток строго односторонний (сервер → клиент), обратный канал не нужен. `gh-webhook` использует WebSocket потому, что пишет ответ локального сервера обратно в GitHub ([разбор](04-github-webhook.md#разбор-cligh-webhook)) — у нас такого требования нет.
- Работает за NAT и корпоративными прокси, дебажится обычным `curl`.
- Авторизация — обычный заголовок `Authorization: Bearer <token>` при подключении ([auth](03-auth.md)).

Эксплуатационные требования:

- Сервер шлёт keepalive-комментарий (`: ping`) каждые ~30 секунд, иначе прокси рвут «тихие» соединения.
- Если перед сервисом nginx/ingress — на этом роуте `proxy_buffering off`.

## Lease / heartbeat

Подписка в SQLite живёт с коротким TTL (`expires_at`, порядок — минуты; точное значение — открытый вопрос):

1. `listen` при старте **переобъявляет** желаемые подписки из локального конфига CLI — идемпотентный upsert через `POST /subscriptions` ([cli](05-cli.md#локальный-конфиг-подписок)).
2. Пока SSE-соединение открыто, сервер сам продлевает lease подписок этого клиента. Само соединение и есть heartbeat — отдельного ping-endpoint нет.
3. Клиент отключился → lease истекает → фоновая чистка (например, раз в час) удаляет протухшие строки.

Чистка — это гигиена данных, а не корректность: офлайн-клиенту SSE всё равно ничего не доставит. Побочный бонус переобъявления: проверка прав на репозиторий происходит при каждом подключении автоматически — см. [auth](03-auth.md#две-точки-проверки).

## Формат события

Черновик (открытый вопрос — см. [README](../../README.md#открытые-вопросы)). Каждое событие — одна SSE-message, `data` — JSON-объект:

```json
{
  "ts": "2026-07-12T16:20:00Z",
  "repo": "org/api",
  "event": "issues",
  "action": "opened",
  "number": 42,
  "subscription_id": 17,
  "payload": { "...": "сырой payload GitHub целиком" }
}
```

- Плоские поля дублируют нормализованный кортеж ([architecture](01-architecture.md#нормализация-payload)) — чтобы фильтровать в `jq` без лазанья вглубь payload.
- `subscription_id` говорит, какая именно подписка сработала (если совпало несколько — событие приходит один раз, поле содержит список либо первую; решить при имплементации).
- CLI печатает `data` как JSON lines в stdout — pipe-friendly ([cli](05-cli.md#listen)).
